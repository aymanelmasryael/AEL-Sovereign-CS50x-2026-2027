# Practical Reverse Engineering: Week 1 - C (Binary Forensics, Symbols & Syscalls)

---

## 🏛️ التشريح الجنائي للملفات التنفيذية (Binary Forensics & Inspection)

في هذا القسم من الهندسة العكسية التطبيقية، لن نكتفي بكتابة الكود المصدري `hello.c` وتشغيله، بل سنقوم بدور **المحقق الجنائي الرقمي (`Binary Forensic Investigator`)** لنفكك الملفات الثنائية الناتجة (`hello.o` و `hello`) ونفهم كيف يراها المعالج ونظام التشغيل.

```text
===================================================================================
                       THE REVERSE ENGINEERING TOOLBOX
===================================================================================

  [ Executable Binary: ./hello ]
            │
            ├─► $ strings hello    ──> Extracts ASCII Literals ("hello, world")
            ├─► $ nm hello         ──> Inspects Symbol Table (_main, _printf)
            ├─► $ objdump -d hello ──> Disassembles Machine Code to Assembly
            └─► $ strace / dtruss  ──> Traces Kernel System Calls (write)

===================================================================================
```

---

## 🔬 أدوات التفكيك الاستقصائية (The Disassembly Suite)

### 1. استخراج النصوص المضمنة (`The strings Command`)
عندما يتم تجميع البرنامج، أين تذهب الجملة النصية `"hello, world\n"`؟ هل تتحول إلى تعليمات معالج؟
*   **التشريح الداخلي:** يتم تخزين السلاسل النصية الثابتة (`String Literals`) في قسم مخصص داخل الملف التنفيذي يسمى **قسم البيانات القراءة فقط (`.rodata - Read-Only Data`)**.
*   **الأمر الاستقصائي:** باستخدام أداة `strings`، يمكننا فحص الملف الثنائي واستخراج كافة النصوص القابلة للقراءة البشرية دون تشغيل البرنامج:
```bash
$ strings hello
...
hello, world
What's your name? 
hello, %s
...
```

### 2. فحص جدول الرموز والعناوين (`The nm Command`)
يحتوي الملف التنفيذي على جدول رموز (`Symbol Table`) يربط أسماء الدوال بعناوينها في الذاكرة.
*   **التشريح الداخلي:** أداة `nm` تكشف لنا الدوال التي تم تعريفها داخلياً (مثل `main`) والدوال التي تم جلبها من مكتبات خارجية (مثل `printf` و `get_string`).
*   **الأمر الاستقصائي:**
```bash
$ nm hello
0000000100003f50 T _main
                 U _printf
                 U _get_string
```
*   **تحليل الرموز:** 
    *   حرف `T` يعني أن الدالة `_main` موجودة ومعرفة في قسم النصوص (`Text Section`) الخاص بملفنا.
    *   حرف `U` يعني أن الدالة `_printf` و `_get_string` غير معرفة داخلياً (`Undefined / External`) وتم ربطها ديناميكياً بمكتبة `C` القياسية (`libc`) ومكتبة `cs50`.

### 3. التفكيك العكسي إلى لغة التجميع (`The objdump Command`)
*   **المهمة التشغيلية:** تحويل الأكواد الثنائية الصفرية (`0s & 1s`) في الملف التنفيذي رجوعاً إلى تعليمات لغة التجميع (`Assembly`).
*   **الأمر الاستقصائي:**
```bash
$ objdump -d hello
hello:  file format Mach-O 64-bit x86-64

Disassembly of section __TEXT,__text:
_main:
100003f50: 55                    pushq %rbp
100003f51: 48 89 e5              movq  %rsp, %rbp
100003f54: 48 8d 3d 55 00 00 00  leaq  85(%rip), %rdi  # loading "hello, world" string
100003f5b: e8 12 00 00 00        callq _printf
100003f60: 31 c0                 xorl  %eax, %eax
100003f62: 5d                    popq  %rbp
100003f63: c3                    retq
```
*   **تحليل الهندسة العكسية:**
    *   `pushq %rbp` و `movq`: إعداد إطار التكدس (`Stack Frame`) لدالة `main`.
    *   `leaq`: تحميل عنوان السلسلة النصية `"hello, world\n"` إلى مسجل المعالج `%rdi` (وهو المسجل المسؤول عن تمرير المعامل الأول للدوال في أنظمة x86_64).
    *   `callq _printf`: استدعاء دالة الطباعة الصريحة.
    *   `xorl %eax, %eax`: تصفير المسجل `%eax` (وهو ما يعادل العبارة الضمنية `return 0;` في نجاح البرنامج).

---

## ⚙️ تتبع نداءات النظام ونواة التشغيل (Tracing Kernel Syscalls)

تعتبر دالة `printf` في لغة C مجرد واجهة تجريدية عالية المستوى. المعالج نفسه لا يعرف كيف يطبع نصاً على شاشة الطرفية. هنا يأتي دور **نداءات النظام (`System Calls`)**.

```text
===================================================================================
                       KERNEL SYSTEM CALL PATHWAY
===================================================================================

  [ Application: printf("hello, world\n") ]
            │
            ▼
  [ C Standard Library: libc (Buffers output) ]
            │
            ▼  (User Mode -> Kernel Mode Switch)
  ┌────────────────────────────────────────────────────────┐
  │ [ OS Kernel Call: write(1, "hello, world\n", 13) ]     │ ──> Writes to File Descriptor 1 (stdout)
  └────────────────────────────────────────────────────────┘
            │
            ▼
  [ Hardware Device / Terminal Display ]

===================================================================================
```

### تشريح مسار الطباعة في النواة:
عند استدعاء `printf("hello, world\n")`، تقوم مكتبة C القياسية بطلب مساعدة نواة نظام التشغيل (`OS Kernel`) عبر نداء نظام قياسي يسمى `write`.
*   **على أنظمة لينكس (`strace`):** أداة `strace ./hello` تعترض هذا النداء وتظهره حرفياً:
    `write(1, "hello, world\n", 13) = 13`
*   **على أنظمة ماك (`dtruss`):** باستخدام `sudo dtruss ./hello`، نرى أن البرنامج يخاطب النواة طالباً كتابة `13` بايت (طول الجملة بالإضافة لرمز `\n`) إلى واصف الملف رقم `1` (`File Descriptor 1`) وهو ما يمثل شاشة المخرجات القياسية `stdout`.

---

## 🧱 التحقيق في الفروق بين `hello.o` و `hello`

| وجه المقارنة | ملف التجميع الوسيط (`hello.o`) | الملف التنفيذي النهائي (`hello`) |
| :--- | :--- | :--- |
| **الاستقلالية التشغيلية** | غير مستقل (`Non-executable`) | مستقل تماماً (`Standalone Executable`) |
| **جدول الرموز (`Symbols`)** | يحتوي على إشارات مبهمة لعناوين الذاكرة | تم استبدال الإشارات بعناوين حقيقية نهائية |
| **قسم البيانات (`.rodata`)** | يحتوي على النصوص الخاصة بالكود المصدري فقط | يحتوي على النصوص مضافاً إليها بيانات الربط |
| **صلاحيات التنفيذ (`Chmod x`)** | يفتقر لصلاحية التنفيذ، هو مجرد حاوية أكواد | يحمل ترويسة التشغيل الصريحة `ELF` أو `Mach-O` |

---
**© Ayman Elmasry — AEL Digital Studio™ · All Rights Reserved.**
